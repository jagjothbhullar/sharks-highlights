{
  "name": "sharks-highlights",
  "private": true,
  "scripts": {
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "seed": "cd server && node scripts/seed.js"
  }
}
